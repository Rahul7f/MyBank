<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rsin.mybank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyBank">
        <activity android:name=".AddMoneyActicity"></activity>
        <activity android:name=".TransferActivity" />
        <activity android:name=".AboutBankActivity" />
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".TransactionHistoryActivity" />
        <activity android:name=".ConformationActivity" />
        <activity android:name=".AccountProfileActivity" />
        <activity android:name=".AccountListActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>